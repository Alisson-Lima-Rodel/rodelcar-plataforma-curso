# Dentro do Docker o host do banco e o servico "db". Backend usa asyncpg:
DATABASE_URL=postgresql+asyncpg://rodelcar:rodelcar@db:5432/rodelcar
JWT_SECRET=troque-este-segredo
# Gere a chave: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
RODELCAR_FERNET_KEY=
ACCESS_TOKEN_EXPIRE_MINUTES=30
